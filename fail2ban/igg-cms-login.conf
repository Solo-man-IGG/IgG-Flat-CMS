# fail2ban filter for IgG CMS login failures
# Matches lines like:
# [2026-06-09 16:30:00] [login] 192.168.1.100 POST /admin/login 401

[Definition]

failregex = ^\[.*\] \[login\] <HOST> POST /admin/login 401
ignoreregex =
